{
  "projects": {
    "floatctl": {
      "canonical": "floatctl",
      "aliases": ["floatctl-rs", "floatctl/evna", "float/evna", "evna-next", "evna"],
      "description": "EVNA-Next MCP server and floatctl Rust toolchain"
    },
    "pharmacy": {
      "canonical": "rangle/pharmacy",
      "aliases": ["pharmacy-online", "pharmonline/pharmacy", "pharmacy"],
      "description": "Rangle pharmacy project (GP node work)"
    }
  },
  "meetings": {
    "scott-sync": {
      "canonical": "pharmacy/scott-sync",
      "aliases": ["scott sync", "scott meeting", "pharmacy sync"],
      "description": "Regular sync meetings with Scott about pharmacy project"
    }
  },
  "_meta": {
    "note": "These are EXAMPLES of typical patterns, not strict rules. User will deviate - fuzzy match generously.",
    "philosophy": "LLMs as fuzzy compilers - normalize the mess, don't enforce rigidity"
  }
}
